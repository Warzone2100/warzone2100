package org.libsdl.app;

import android.util.Log;
import android.view.InputDevice;
import android.view.MotionEvent;

/**
 * SDL3 Android controller/gamepad manager (SDL3 3.2.x Java layer).
 *
 * Stub for Phase A. Full gamepad support is part of Phase C.
 */
class SDLControllerManager {

    private static final String TAG = "SDL";

    public SDLControllerManager() {
        Log.v(TAG, "SDLControllerManager created");
    }
}
