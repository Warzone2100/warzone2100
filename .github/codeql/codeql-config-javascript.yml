name: "WZ CodeQL JS config"

# Interpreted languages scanning configuration (ex. JavaScript/TypeScript)
paths:
  - data

paths-ignore: 
  - 3rdparty
  - tests
  - tools

queries:
  - uses: security-extended
  - uses: security-and-quality

